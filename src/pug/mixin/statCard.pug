mixin statCard(modifierClass, img, label, num, unit)
	li.stat-card(class=modifierClass)
		.stat-card__img
			img(src=img, alt="")
		.stat-card__text
			span.stat-card__label= label
			p.stat-card__num
				strong.stat-card__count(data-count=num.replace(/,/g, ''))= num
				span= unit
