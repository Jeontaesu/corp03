mixin contentTitle(title, subText)
    .main__section-title
        if subText
            p.main__sub-text(data-animation="slideInLeft") !{subText}
        h2.main__title(data-animation="slideInLeft") !{title}